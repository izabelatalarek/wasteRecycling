#include "Garbage.h"
#include <iostream>



void Dustbin9000:: acceptCaps(Cap inCap, std::vector<Cap> capsContent)
{
	if (capColor == "pink")
	{
		capsContent.push_back(inCap);
	}
	else {};
}

